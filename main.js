// document.getElementById('delimitButton').addEventListener('click', function() {
//     let inputText = document.getElementById('inputText').value;
//     let delimiter = document.getElementById('delimiter').value;
//     let resultText = delimitText(inputText, delimiter);
//     document.getElementById('resultText').innerText = resultText;
// });

// function delimitText(text, delimiter) {
//     // Split the text by new lines, then join with the selected delimiter
//     return text.split('\n').join(delimiter + '\n');
// }